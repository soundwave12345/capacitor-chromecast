{
  "name": "capacitor-chromecast-android",
  "version": "0.1.3",
  "description": "Capacitor plugin (Android only) to cast media to Chromecast devices.",
  "main": "dist/index.cjs.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "android",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node ./scripts/copy-readme.js || true",
    "prepare": "npm run build",
    "lint": "eslint . --ext .ts",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "keywords": [
    "capacitor",
    "chromecast",
    "android",
    "plugin"
  ],
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "eslint": "^8.0.0"
  },
  "capacitor": {
    "android": {
      "src": "android"
    }
  }
}
